extends layout

block content
  .container
    .row.justify-content-center
      .col-10
        table.table.table-dark
          thead
            tr
              th(scope='col') #
              th(scope='col') Name
              th(scope='col') Amount
              th(scope='col') Category
          tbody
            each expense in expenses
              tr
                th(scope='row')= expense.id
                td= expense.name
                td= expense.amount
                td= expense.category