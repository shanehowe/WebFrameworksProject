extends layout

block content
  
  .container.mt-5.text-center
    h1 Welcome to Expense Tracker

    .row.justify-content-center.mt-4
      .col-auto
        a.btn.btn-primary.btn-lg(href='/auth/register') Register
      .col-auto
        a.btn.btn-secondary.btn-lg(href='/auth/login') Login